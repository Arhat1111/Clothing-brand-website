# Admin link privacy fix

The public website navigation no longer links to `admin.html`.

The admin dashboard is still available only by opening the private direct URL shared with the owner:

```
/admin.html
```

The page remains protected by the admin passcode and the Vercel `ADMIN_TOKEN` for live orders.
